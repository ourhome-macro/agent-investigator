# 2026-09-15 提交说明

## 提交范围

本次将竞品研究闭环、来源分级和工作台可读性改造一起提交至
`ourhome-macro/agent-investigator` 的 `main` 分支。

- 正文先于候选筛选抓取；候选快照、准入结果和结论引用保留来源关系。
- 覆盖缺口与审计问题驱动补采、修订、拆分和排除；审计按任务分块。
- 调查预算先预留再结算，模型调用前限制文本输入与输出额度。
- 来源分级允许可信官方单来源，厂商自述和用户反馈始终保留限定。
- 普通备注和可接受缺口不冻结报告；必答项目和基本竞品事实仍受门禁保护。
- 工作台使用中文进度和来源说明，技术编号与原始错误折叠展示。
- 新增调查迁移 `ci_0010`、`ci_0011` 和相关回归测试。

## 文档整理

- 根 README 改为 Agent Investigator 的项目介绍、上手步骤与结果阅读说明。
- 继承的通用说明保留为根目录 `DEERFLOW_REFERENCE.md`，原有相对资源路径保持有效；去掉其中重复的研究子系统说明。
- 新增 `docs/README.md`，区分当前规则、使用指南、验收记录与历史分析。
- 新增 Windows PowerShell 启动指南，所有宿主机前端命令使用 `scripts/pnpm.py`。
- 更新现行规格与编排说明，修正文档里统一双域名、固定六个搜索角度、浏览器打印和自动重试等过时表述。
- 历史诊断和计划保留原路径并加适用范围说明，已有引用继续可用。

## 验证与提交边界

本次推送前检查：

- 相关后端回归 **393 passed in 22.66s**（研究子系统、Gateway、预算中间件、Subagent Executor/Batch Service 和 harness 边界）。
- 前端工作台 **5 项测试通过**；`pnpm check` 的 ESLint 与 TypeScript 检查通过。
- 后端全目录 Ruff lint 通过，**1448 个 Python 文件格式检查通过**。
- 新增/修改的 **23 份 Markdown 文件**本地链接检查通过，链接目标均存在于 Git 索引。
- 暂存文件检查未发现本地配置、凭据或运行数据路径；新增内容的凭据特征扫描无匹配。此扫描不替代人工审查。

全库普通套件存在已记录的 Windows `os.geteuid` 收集问题；严格 I/O 套件的
五项平台相关失败也已记录。真实 Provider 和生产集群验收没有完成，不能把
离线通过等同于生产就绪。

本地凭据、真实配置、运行数据库、日志与依赖不纳入提交；保留既有本机文档
排除规则。提交 GitHub 不执行生产部署。
